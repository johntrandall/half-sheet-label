"""half-sheet-label — impose a rendered label PDF onto half-sheet 2-up stock.

See cli.py for the entry point. The package is intentionally thin: geometry in
`impose.py`, label-content trimming in `trim.py`, config in `config.py`,
orchestration in `cli.py`.
"""

__version__ = "0.2.0"
